{
  "slug": "ATTESTED_BY__sigstore_core",
  "name": "Attested By",
  "description": "Signed entity's Sigstore signature was recorded in Rekor at this log entry, verified under the policy captured in the edge attributes at the captured time. Verdict + applied policy live on this edge (not on the rekor_log_entry node) because verification is a fact about (artifact bytes + bundle + policy + verification time), not an immutable property of the log entry.",
  "targets": ["sigstore_core__rekor_log_entry"],
  "property_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["signature_verified", "verified_at", "policy_kind", "policy_oidc_issuer"],
    "properties": {
      "signature_verified": {
        "type": "boolean",
        "description": "Absolute verdict from verify_bundle. True if all of signature, cert chain, Rekor inclusion proof, and the supplied policy match."
      },
      "verified_at": {
        "type": "string",
        "description": "ISO 8601 UTC of the verification call that produced this verdict."
      },
      "policy_kind": {
        "type": "string",
        "description": "Kind of policy applied. v0 emits only 'github_workflow'."
      },
      "policy_oidc_issuer": {
        "type": "string",
        "description": "OIDC issuer URL the policy required."
      },
      "policy_github_repository": {
        "type": "string",
        "description": "GitHub repo the policy required (when policy_kind = github_workflow)."
      },
      "policy_workflow_identity_uri": {
        "type": "string",
        "description": "Exact SAN URI the policy required, when supplied."
      },
      "policy_workflow_ref": {
        "type": "string",
        "description": "Git ref the policy required, when supplied."
      },
      "policy_workflow_sha": {
        "type": "string",
        "description": "Commit SHA the policy required, when supplied."
      },
      "verification_failure_code": {
        "type": "string",
        "description": "Machine-readable failure tag when signature_verified is false: bundle_parse, no_rekor_proof, policy_mismatch, cert_chain, rekor_proof, signature, unexpected. Empty when verified."
      },
      "verification_failure_detail": {
        "type": "string",
        "description": "Human-readable failure detail from the verifier when signature_verified is false. Empty when verified."
      }
    }
  },
  "default_dimensions": {
    "sigstore.platform": "public-good"
  }
}
